Fix collision physics for obstacles when colliding with player to properly apply linear velocity

Update behavior when "soft-colliding" (or pushing) obstacles already hit

Add particles behind player car when driving and veering left/right

Change car invincibility to not be an animation and instead be driven with code so that other animations can play properly after a collision